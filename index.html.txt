<!DOCTYPE html>
<html lang="ar" dir="ltr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Instagram Login</title>
  <link href="https://fonts.googleapis.com/css2?family=Grand+Hotel&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet"/>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: #fafafa;
      font-family: 'DM Sans', sans-serif;
    }

    body::before {
      content: '';
      position: fixed;
      top: -200px; left: -200px;
      width: 600px; height: 600px;
      border-radius: 50%;
      background: radial-gradient(circle, #f09433 0%, #e6683c 25%, #dc2743 50%, #cc2366 75%, #bc1888 100%);
      opacity: 0.08;
      filter: blur(80px);
      pointer-events: none;
    }

    .wrapper {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 12px;
      width: 100%;
      max-width: 360px;
      padding: 0 20px;
      animation: fadeUp 0.5s ease both;
    }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(24px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    .card {
      width: 100%;
      background: #fff;
      border: 1px solid #dbdbdb;
      border-radius: 4px;
      padding: 40px 36px 24px;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .logo {
      font-family: 'Grand Hotel', cursive;
      font-size: 52px;
      background: linear-gradient(45deg, #f09433, #e6683c, #dc2743, #cc2366, #bc1888);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: 28px;
      letter-spacing: -1px;
    }

    form { width: 100%; display: flex; flex-direction: column; gap: 6px; }

    .input-wrap { position: relative; width: 100%; }

    .input-wrap input {
      width: 100%;
      padding: 9px 12px;
      padding-right: 40px;
      border: 1px solid #dbdbdb;
      border-radius: 4px;
      background: #fafafa;
      font-size: 12px;
      color: #262626;
      outline: none;
      transition: border-color 0.2s;
    }

    .input-wrap input::placeholder { color: #8e8e8e; }
    .input-wrap input:focus { border-color: #a8a8a8; background: #fff; }

    .toggle-pw {
      position: absolute;
      right: 10px; top: 50%;
      transform: translateY(-50%);
      background: none; border: none;
      cursor: pointer;
      font-size: 12px;
      font-weight: 600;
      color: #262626;
      padding: 0;
    }

    .divider {
      display: flex;
      align-items: center;
      gap: 16px;
      margin: 14px 0;
      width: 100%;
    }
    .divider::before, .divider::after {
      content: '';
      flex: 1;
      height: 1px;
      background: #dbdbdb;
    }
    .divider span {
      font-size: 12px;
      font-weight: 600;
      color: #8e8e8e;
      letter-spacing: 1px;
    }

    .btn-fb {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      width: 100%;
      background: none;
      border: none;
      cursor: pointer;
      color: #385185;
      font-size: 14px;
      font-weight: 600;
      font-family: 'DM Sans', sans-serif;
      padding: 6px 0;
      transition: opacity 0.2s;
    }
    .btn-fb:hover { opacity: 0.7; }

    .forgot { text-align: center; margin-top: 12px; }
    .forgot a { font-size: 12px; color: #262626; text-decoration: none; }
    .forgot a:hover { text-decoration: underline; }

    .btn-login {
      width: 100%;
      margin-top: 8px;
      padding: 8px;
      background: linear-gradient(45deg, #f09433, #e6683c, #dc2743, #cc2366, #bc1888);
      border: none;
      border-radius: 8px;
      color: #fff;
      font-size: 14px;
      font-weight: 700;
      font-family: 'DM Sans', sans-serif;
      cursor: pointer;
      transition: opacity 0.2s, transform 0.1s;
    }
    .btn-login:hover { opacity: 0.9; }
    .btn-login:active { transform: scale(0.98); }
    .btn-login:disabled { opacity: 0.4; cursor: not-allowed; }

    .card-signup {
      width: 100%;
      background: #fff;
      border: 1px solid #dbdbdb;
      border-radius: 4px;
      padding: 20px;
      text-align: center;
      font-size: 14px;
      color: #262626;
    }
    .card-signup a { color: #0095f6; font-weight: 700; text-decoration: none; }
    .card-signup a:hover { text-decoration: underline; }

    .app-badges {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 12px;
      margin-top: 8px;
    }
    .app-badges p { font-size: 13px; color: #262626; }
    .badges-row { display: flex; gap: 8px; }
    .badge {
      display: flex;
      align-items: center;
      gap: 6px;
      padding: 6px 12px;
      border: 1px solid #dbdbdb;
      border-radius: 6px;
      text-decoration: none;
      color: #262626;
      font-size: 12px;
      font-weight: 500;
      transition: background 0.2s;
    }
    .badge:hover { background: #f5f5f5; }
  </style>
</head>
<body>

<div class="wrapper">

  <div class="card">
    <div class="logo">Instagram</div>

    <form id="loginForm" novalidate>
      <div class="input-wrap">
        <input type="text" id="username" placeholder="Phone number, username, or email" required />
      </div>

      <div class="input-wrap">
        <input type="password" id="password" placeholder="Password" required />
        <button type="button" class="toggle-pw" id="togglePw">Show</button>
      </div>

      <button type="submit" class="btn-login" id="loginBtn" disabled>Log in</button>

      <div class="divider"><span>OR</span></div>

      <button type="button" class="btn-fb">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="#385185">
          <path d="M24 12.073C24 5.405 18.627 0 12 0S0 5.405 0 12.073C0 18.1 4.388 23.094 10.125 24v-8.437H7.078v-3.49h3.047V9.41c0-3.025 1.792-4.697 4.533-4.697 1.312 0 2.686.236 2.686.236v2.97h-1.513c-1.491 0-1.956.93-1.956 1.883v2.271h3.328l-.532 3.49h-2.796V24C19.612 23.094 24 18.1 24 12.073z"/>
        </svg>
        Log in with Facebook
      </button>

      <div class="forgot"><a href="#">Forgot password?</a></div>
    </form>
  </div>

  <div class="card-signup">
    Don't have an account? <a href="#">Sign up</a>
  </div>

  <div class="app-badges">
    <p>Get the app.</p>
    <div class="badges-row">
      <a href="#" class="badge">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
          <path d="M18.71 19.5c-.83 1.24-1.71 2.45-3.05 2.47-1.34.03-1.77-.79-3.29-.79-1.53 0-2 .77-3.27.82-1.31.05-2.3-1.32-3.14-2.53C4.25 17 2.94 12.45 4.7 9.39c.87-1.52 2.43-2.48 4.12-2.51 1.28-.02 2.5.87 3.29.87.78 0 2.26-1.07 3.8-.91.65.03 2.47.26 3.64 1.98-.09.06-2.17 1.28-2.15 3.81.03 3.02 2.65 4.03 2.68 4.04-.03.07-.42 1.44-1.38 2.83M13 3.5c.73-.83 1.94-1.46 2.94-1.5.13 1.17-.34 2.35-1.04 3.19-.69.85-1.83 1.51-2.95 1.42-.15-1.15.41-2.35 1.05-3.11z"/>
        </svg>
        App Store
      </a>
      <a href="#" class="badge">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
          <path d="M3.18 23.76c.3.17.64.24.99.21l12.44-12.44L13.15 8.1 3.18 23.76zm16.09-10.7L16.44 11.5l3.22-3.22c.56-.35.87-.96.87-1.6 0-.94-.62-1.74-1.5-1.97L5.19.21C4.84.07 4.46.04 4.1.14L16.44 12.5l2.83-1.44zM2.07 1.32C2.02 1.54 2 1.77 2 2v20c0 .23.02.46.07.68l12.44-12.44L2.07 1.32zm14.71 8.5l-2.34 2.34 2.34 2.34 3.5-2.01c.56-.35.87-.96.87-1.6 0-.65-.3-1.26-.87-1.6l-3.5-1.47z"/>
        </svg>
        Google Play
      </a>
    </div>
  </div>

</div>

<script>
  const usernameInput = document.getElementById('username');
  const passwordInput = document.getElementById('password');
  const loginBtn = document.getElementById('loginBtn');
  const togglePw = document.getElementById('togglePw');
  const loginForm = document.getElementById('loginForm');

  function checkInputs() {
    const filled = usernameInput.value.trim() !== '' && passwordInput.value.trim() !== '';
    loginBtn.disabled = !filled;
  }
  usernameInput.addEventListener('input', checkInputs);
  passwordInput.addEventListener('input', checkInputs);

  togglePw.addEventListener('click', () => {
    const isPassword = passwordInput.type === 'password';
    passwordInput.type = isPassword ? 'text' : 'password';
    togglePw.textContent = isPassword ? 'Hide' : 'Show';
  });

  loginForm.addEventListener('submit', (e) => {
    e.preventDefault();
    loginBtn.textContent = 'Logging in...';
    loginBtn.disabled = true;
    setTimeout(() => {
      alert('Welcome back, ' + usernameInput.value + '! ✅');
      loginBtn.textContent = 'Log in';
      loginBtn.disabled = false;
    }, 1200);
  });
</script>
</body>
</html>